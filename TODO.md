# TODO - Loyverse API Frontend (Dummy stock editor)

- [x] Moved UI flow into `src/pages/Inventory.tsx` (dummy stock editor).




- [x] Define local dummy types + dummy data inside `Reports.tsx` (no API).

- [x] Add UI: product search, product table, branch/employee stock matrix, edit via input + save button.

- [x] Keep build passing (remove old inventory-status tab logic).

- [ ] Run `npm run build` and fix any TS errors.

