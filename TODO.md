# TODO — Frontend changes

- [x] Remove audit API calls; keep mock/empty state for Dashboard/Reports.
- [x] Add Audit filters: Branch + Movement (Decrease/Increase) UI.
- [x] Update audit filter logic in `useAuditFilters` to filter by branch + movement.
- [x] Wire new filter props through `Dashboard.tsx` and `AuditDashboard.tsx`.
- [x] Fix filter UI rendering in `AuditFilters.tsx` (dropdowns actually visible).
- [ ] Update `AGENTS.md` to reflect current UI flow and system context.
- [ ] Start new frontend flow (Products list + editable stock per branch/employee) using dummy data first.

